versioning:
  baseUrl: clients/client-settings
  current: native
  versions:
    - native
    - single-page-app
    - web-app
    - non-interactive
  defaultArticles:
    native: index
    single-page-app: index
    web-app: index
    non-interactive: index
